# packages à installer quand on démarre Onyxia

install.packages(c("questionr","report","kableExtra","BioStatR"))


#Pour ne pas rentrer le mot de passe dans GIT à chaque fois
#git config --global credential.helper store